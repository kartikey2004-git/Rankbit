name: "check ranking for all keywords"
on:
  schedule:
    - cron: "0 0 * * *"
  jobs:
    cron:
     runs-on: ubuntu-latest
     steps:
       - name: Call our api route
         run: curl 'https://gkhjos-ip-106-221-235-141.tunnelmole.net/api/check-for-all-keywords'

       